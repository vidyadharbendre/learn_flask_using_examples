# Each example directory is a standalone Flask app with its own app.py, so they
# cannot be type checked in a single mypy run (duplicate module "app"). Use
# ./typecheck.sh, which checks one example at a time with this config.
[mypy]
strict = True
exclude = (?x)(__pycache__/)
